package com.patio.drivers;

import com.patio.config.DeviceConfig;
import org.openqa.selenium.WebDriver;

public class TabletDriverFactory {
    public static WebDriver createDriver(DeviceConfig device) {
        return null;
    }
}
